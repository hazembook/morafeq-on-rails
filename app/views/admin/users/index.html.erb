<div class="max-w-5xl mx-auto">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-bold"><%= t("admin.users.index.title") %></h1>
    <%= link_to t("admin.users.index.new"), new_admin_user_path, class: "rounded-md bg-blue-600 px-4 py-2 text-white text-sm font-medium hover:bg-blue-700" %>
  </div>

  <div class="bg-white rounded-lg shadow overflow-x-auto">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50">
        <tr>
          <th class="px-6 py-3 text-start text-xs font-medium text-gray-500 uppercase"><%= t("admin.table.name") %></th>
          <th class="px-6 py-3 text-start text-xs font-medium text-gray-500 uppercase"><%= t("admin.table.email") %></th>
          <th class="px-6 py-3 text-start text-xs font-medium text-gray-500 uppercase"><%= t("admin.table.role") %></th>
          <th class="px-6 py-3 text-end text-xs font-medium text-gray-500 uppercase"><%= t("common.actions") %></th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-200">
        <% @users.each do |user| %>
          <tr>
            <td class="px-6 py-4"><%= user.full_name %></td>
            <td class="px-6 py-4"><%= user.email_address %></td>
            <td class="px-6 py-4"><%= user.role.titleize %></td>
            <td class="px-6 py-4 text-end rtl:space-x-reverse space-x-2">
              <%= link_to t("admin.actions.show"), admin_user_path(user), class: "text-blue-600 hover:text-blue-800 text-sm" %>
              <%= link_to t("common.edit"), edit_admin_user_path(user), class: "text-blue-600 hover:text-blue-800 text-sm" %>
              <%= button_to t("common.delete"), admin_user_path(user), method: :delete, class: "text-red-600 hover:text-red-800 text-sm", data: { turbo_confirm: t("common.confirm_delete") } %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
